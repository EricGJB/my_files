# 网络信息开放赋能AI计算资源编排与调度——研究报告

## 一、问题视角反转

上一份报告聚焦"网络如何基于应用信息优化自身资源分配"。本报告反转视角：**无线网络可以向应用层/计算平台开放哪些信息，使其更好地编排AI计算资源、调度推理任务、提升端到端QoE？**

核心逻辑：无线网络是连接用户与云端AI服务的唯一通道，掌握着应用层无法获取的实时链路状态信息。如果这些信息能有效暴露给计算调度器，就可以实现**网络-计算联合优化**，而非各自独立决策。

---

## 二、无线网络可开放的关键信息类别

### 2.1 无线信道状态信息 (Channel State Information)

| 信息项 | 描述 | 更新频率 | 对计算调度的价值 |
|--------|------|----------|------------------|
| **CQI/RI/PMI** | 当前信道质量指示、秩指示、预编码矩阵 | 每slot~每20ms | 预测上行/下行可用吞吐量 |
| **SINR** | 信噪比 | 实时 | 判断用户当前链路是否支持高吞吐prefill上行 |
| **Pathloss** | 路径损耗 | 秒级 | 估算用户距离和信号衰减，预测链路稳定性 |
| **Doppler shift** | 多普勒频移 | 秒级 | 判断用户移动速度，预测信道变化趋势 |

**计算调度价值**：
- 信道质量好 → 可分配prefill大块上行 → 预测prefill传输时间
- 信道质量差 → 需要更多HARQ重传 → 预测上行延迟增加 → 选择边缘推理而非云端
- 高移动性 → 链路不稳定 → 预测任务完成时间不确定性 → 增加冗余计算资源

### 2.2 资源调度与拥塞状态

| 信息项 | 描述 | 对计算调度的价值 |
|--------|------|------------------|
| **当前PRB利用率** | 小区/用户级资源使用比例 | 预测新请求的调度等待时间 |
| **UL/DL queue depth** | 上下行排队深度 | 判断当前拥塞程度，是否需要切换边缘节点 |
| **调度延迟历史** | 过去N个slot的平均调度延迟 | 预测未来调度延迟趋势 |
| **Preemption机会** | 是否可抢占低优先级流量 | 判断AI流量能否获得紧急调度 |

**计算调度价值**：
- PRB利用率>80% → 新AI请求延迟将显著增加 → 调度器应选择负载较低的边缘节点
- UL queue depth大 → 大块prefill上行将排队 → 可能需要降低prompt粒度或切换到预计算结果

### 2.3 端到端延迟与RTT信息

| 信息项 | 描述 | 对计算调度的价值 |
|--------|------|------------------|
| **RTT测量** | 用户到服务端的实时RTT | TTFT = RTT + Prefill计算 + 首token传输；网络侧RTT直接影响TTFT |
| **RTT jitter** | RTT抖动 | TPOT稳定性受jitter影响；高jitter需更多token缓冲 |
| **One-way delay** | 单向延迟(UL/DL分离) | 区分UL延迟和DL延迟的贡献 |
| **RTT预测** | 基于历史+当前负载的RTT预测 | 提前告知计算平台"用户将在X ms后收到首个token" |

**计算调度价值**：
- 这是最直接的闭环：**计算平台根据网络RTT预测调整token生成策略**
- RTT高 → 服务端可加速prefill计算（投入更多GPU资源）以补偿网络延迟
- RTT jitter大 → 服务端可增大decode缓冲区，以smooth streaming

### 2.4 用户移动性与位置信息

| 信息项 | 描述 | 对计算调度的价值 |
|--------|------|------------------|
| **用户速度/方向** | 移动速度和方向 | 预测用户是否即将切换到邻区，计算任务是否需要迁移 |
| **当前服务小区/邻区RSRP** | 服务和邻区信号强度 | 判断用户是否在小区边缘（资源受限） |
| **切换预测** | 基于速度和位置的切换概率 | 提前准备计算任务迁移，避免切换期间的推理中断 |
| **轨迹预测** | 用户移动轨迹 | 预测用户未来将到达哪个边缘节点的服务范围 |

**计算调度价值**：
- 用户高速移动 → 可能即将切换 → 计算任务应在当前边缘节点加速完成，而非启动新任务
- 用户静止在小区边缘 → 资源受限 → 云端推理可能更可靠
- 轨迹预测 → 提前在目标边缘节点预加载模型/KV Cache

### 2.5 网络切片与QoS状态

| 信息项 | 描述 | 对计算调度的价值 |
|--------|------|------------------|
| **切片ID与SLA** | 当前用户所在切片的QoS保障 | 不同切片有不同延迟/吞吐保障，计算调度据此选择 |
| **切片可用资源** | 切片内剩余PRB/带宽 | 判断当前切片是否足以支撑AI推理流量 |
| **切片间负载均衡** | 各切片的负载对比 | 是否需要将AI流量迁移到负载更低的切片 |

### 2.6 HARQ与可靠性信息

| 信息项 | 描述 | 对计算调度的价值 |
|--------|------|------------------|
| **BLER (Block Error Rate)** | 当前BLER | 高BLER意味着需要更多重传，传输时间不确定 |
| **HARQ重传次数** | 当前传输的HARQ round | 判断链路可靠性，预测最终传输成功概率 |
| **RLC重传统计** | RLC层重传情况 | 无线链路层面的端到端可靠性 |

---

## 三、信息开放的技术实现路径

### 3.1 3GPP NEF (Network Exposure Function)

5G核心网中NEF是标准的网络信息开放接口：

| NEF能力 | 信息开放方式 | 适用场景 |
|---------|-------------|----------|
| **AF influence on traffic routing** | AF请求网络调整QoS/路由 | 计算平台请求为AI流量提升优先级 |
| **Network status information** | 通过Namf/Nsmf暴露网络状态 | 向MEC暴露当前小区负载/延迟 |
| **QoS flow control** | 动态调整QoS参数 | 根据AI推理阶段动态调整QoS |

**局限**：NEF目前主要暴露核心网层面信息，对RAN侧实时信息的暴露能力有限。5G-Advanced/6G需要扩展RAN信息暴露。

### 3.2 O-RAN RIC (RAN Intelligent Controller)

O-RAN架构的RIC提供了更细粒度的RAN信息暴露：

| RIC能力 | 信息暴露 | AI计算调度价值 |
|---------|----------|----------------|
| **xApp on Near-RT RIC** | 实时RAN状态(每10ms-1s) | 信道质量、调度延迟、资源利用率 |
| **rApp on Non-RT RIC** | 非实时RAN analytics(>1s) | 长期趋势、用户行为模式、容量预测 |
| **E2接口** | gNB内部状态 | PRB分配、HARQ统计、beam管理 |
| **A1接口** | 策略下发 | 计算平台可通过A1下发AI流量优先级策略 |

**关键优势**：RIC架构天然支持将RAN信息暴露给第三方应用(xApp/rApp)，计算调度器可以作为xApp运行在Near-RT RIC上，获取毫秒级RAN状态。

### 3.3 ITU-T Y.317x 系列——网络性能信息开放

ITU-T SG13定义了网络信息开放的标准框架：

| 标准 | 内容 | AI计算价值 |
|------|------|-----------|
| **Y.3176** | Network performance measurement | 端到端延迟/吞吐/抖动 |
| **Y.3172** | Architectural framework for network ML | 网络ML能力暴露 |
| **Y.3174** | Data handling for network ML | 训练数据/推理结果的标准化交换 |

### 3.4 3GPP RAAP (RAN Application API)

6G研究中提出的RAN应用编程接口，可将RAN实时状态以标准化API形式暴露给上层应用：

| API类型 | 暴露信息 | 使用者 |
|---------|----------|--------|
| **RAN QoS API** | 当前无线链路QoS状态 | MEC计算调度器 |
| **RAN Load API** | 小区/用户级资源负载 | 云端任务调度器 |
| **RAN Prediction API** | 未来N ms的信道预测 | 预计算资源预留 |

---

## 四、信息开放赋能计算调度的具体场景

### 场景1：AI推理任务的边缘-云端动态路由

**问题**：当用户发起AI推理请求时，计算平台需要决定在边缘还是云端执行。但计算平台不掌握无线链路状态。

**网络信息开放方案**：

```
用户发起请求 → 网络暴露当前信息给计算调度器：
  - 当前RTT: 15ms
  - 当前UL PRB利用率: 30%
  - 当前BLER: 1%
  - 用户速度: 3km/h (静止)
  → 调度器决策：
    - RTT低、信道好 → 边缘推理（prefill在边缘，decode在边缘/云）
    - RTT高或即将切换 → 云端推理（避免边缘任务迁移中断）
```

**预期收益**：避免在信道差时启动边缘推理（导致prefill超时），也避免在信道好时不必要地使用云端（增加RTT）。

### 场景2：Prefill传输时间预测与GPU资源预留

**问题**：Prefill阶段需要将整个prompt（可能数千token）上行传输到服务端。传输时间取决于无线信道质量。

**网络信息开放方案**：

```
网络暴露信息给计算调度器：
  - 用户当前UL吞吐量: 50 Mbps
  - prompt大小: 8000 tokens ≈ 32KB
  - 预测prefill上行传输时间: ~5ms
  → GPU调度器：
    - 在prefill传输完成前不分配GPU（避免浪费）
    - 在prefill传输即将完成时启动GPU warm-up
    - TTFT目标 = 传输时间 + Prefill计算时间 + 首token传输时间
```

**关键**：网络提供**"数据到达时间预测"**，使GPU调度器可以**精确安排计算资源**，而非盲目预留。

### 场景3：Token Streaming的网络-计算速率协调

**问题**：Decode阶段的token生成速率与无线下行调度可能不匹配。

**网络信息开放方案**：

```
网络暴露信息给token streaming服务：
  - 当前DL可用吞吐量: 100 Mbps
  - 当前DL调度延迟: 2ms
  - 当前DL jitter: 1ms
  → Token streaming服务：
    - 可安全以最高token rate发送（100Mbps >> token rate需求）
    - 如果DL jitter增大 → 增大客户端缓冲区
    - 如果DL吞吐量下降 → 降低token发送rate，避免buffer溢出
```

### 场景4：多用户AI并发的全局调度

**问题**：大量用户同时请求AI推理，GPU资源有限。计算平台需要决定优先服务哪些用户。

**网络信息开放方案**：

```
网络暴露全局信息给计算调度器：
  - 各用户当前信道质量排名
  - 各用户当前调度延迟
  - 各用户移动性(速度)
  - 各用户SLO紧迫度(TTFT deadline)
  → 全局调度器：
    - 信道好的用户 → 可以容忍更长GPU排队（因为传输快）
    - 信道差的用户 → 优先分配GPU（补偿传输延迟）
    - 即将切换的用户 → 加速完成当前任务或放弃
    - TTFT deadline紧迫的用户 → 抢占低优先级任务
```

### 场景5：AI模型分片与边缘预取

**问题**：大模型（如70B+参数）无法完整部署在边缘节点。

**网络信息开放方案**：

```
网络暴露用户位置和移动性信息：
  - 用户当前位于A小区，速度5km/h，预计停留2分钟
  - A小区边缘节点有模型前60层
  → 模型编排器：
    - 将模型后10层预加载到A小区边缘节点
    - 用户prefill在A小区完成前60层计算
    - 后10层通过回传从邻区B获取（因为用户可能向B移动）
    - 如果用户静止 → 全部在A完成
```

---

## 五、信息开放的挑战与设计考虑

### 5.1 信息时效性

| 信息类型 | 典型更新周期 | 过时容忍度 |
|----------|-------------|-----------|
| CQI/RI | 5-20ms | <100ms |
| PRB利用率 | 10-100ms | <500ms |
| RTT | 10-50ms | <200ms |
| 用户速度 | 100ms-1s | <5s |
| 切换预测 | 100ms-1s | <2s |

**设计要求**：信息开放API必须支持**实时订阅+推送模式**，而非传统的请求-响应模式。

### 5.2 信息粒度

- **Too coarse** (如小区级平均负载) → 计算调度无法做出精确决策
- **Too fine** (如每个PRB的状态) → 信息量过大，处理开销高
- **建议粒度**：用户级 + 100ms时间粒度，对AI计算调度已足够

### 5.3 隐私与安全

- 用户位置/移动性信息暴露给计算平台需要**用户授权**
- 信息暴露应通过**标准化接口**（NEF/RAAP），避免私有接口碎片化
- 敏感信息（如精确位置）应支持**差分隐私**或**模糊化**处理

### 5.4 跨域协同

- RAN信息（xApp获取）→ 需要传递到Core网络（NEF）→ 再暴露给应用
- 跨运营商场景：漫游用户的网络信息如何跨域暴露
- 云-边-端三方协同：信息需要在三个域之间流通

---

## 六、总结：网络信息开放的技术研究路线

| 优先级 | 方向 | 核心信息 | 接收方 | 预期价值 |
|--------|------|----------|--------|----------|
| **P0** | RTT/延迟实时暴露 | RTT, jitter, one-way delay | Token streaming服务 | 直接优化TTFT/TPOT |
| **P0** | 信道质量+吞吐预测 | CQI, SINR, UL/DL throughput预测 | 计算任务路由器 | 边缘/云决策 |
| **P1** | RAN负载状态暴露 | PRB利用率, queue depth, scheduling delay | GPU调度器 | 计算资源预分配 |
| **P1** | 用户移动性信息 | 速度, 方向, 切换预测 | 模型编排器 | 任务迁移/预取 |
| **P2** | HARQ/可靠性统计 | BLER, 重传次数, RLC重传 | 自适应编码器 | 调整传输冗余 |
| **P2** | 网络切片状态 | 切片SLA, 可用资源, 负载 | 多切片调度器 | 跨切片负载均衡 |

**一句话总结**：无线网络不仅是"管道"，更是"传感器"——它掌握着应用层无法获取的实时链路状态。将这些信息标准化暴露给AI计算调度平台，是实现**网络-计算联合智能**的关键一步。
